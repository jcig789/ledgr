{
  "name": "ledgr",
  "version": "0.1.0",
  "description": "Multi-currency personal finance for Obsidian. Track spending, budgets, net worth, and international transfers — all in your vault.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": ["obsidian", "finance", "budgeting", "multi-currency", "net-worth", "personal-finance"],
  "author": "jcig789",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.21.5",
    "obsidian": "latest",
    "tslib": "2.6.2",
    "typescript": "^5.5.3"
  }
}
